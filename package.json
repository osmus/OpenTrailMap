{
  "name": "opentrailmap",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/buildStyle.js",
    "preview": "vite preview",
    "sprites": "node scripts/buildSprites.js",
    "test": "node scripts/validateStyles.js",
    "lint": "biome lint",
    "format": "biome format --write",
    "check": "biome check"
  },
  "dependencies": {
    "maplibre-gl": "^6.6.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11",
    "@maplibre/maplibre-gl-style-spec": "^24.10.0",
    "vite": "^8.2.2"
  }
}
